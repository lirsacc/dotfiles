#!/usr/bin/env bash

# Make sure we’re using the latest npm and update existing packages
npm update

npm install -g grunt-cli
npm install -g gulp
npm install -g bower
npm install -g mocha
npm install -g istanbul
npm install -g browserify
npm install -g gitignore
npm install -g vtop
npm install -g fsmonitor
